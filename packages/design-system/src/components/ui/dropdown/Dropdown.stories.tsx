import { Dropdown } from './Dropdown';

const meta = {
  title: 'Design System/UI/Dropdown',
  component: Dropdown,
};

export default meta;

export const Default = {
  args: {
    children: 'Dropdown',
  },
};
