# Design System Components

Public components are grouped by responsibility.

- `ui`: product-agnostic UI primitives
- `layout`: product-agnostic layout primitives

Use `pnpm gen:ds-component --args <ComponentName> <ui|layout> <component-folder>` to create a component scaffold.
