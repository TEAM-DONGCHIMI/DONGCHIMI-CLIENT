interface:
  display_name: '프론트엔드 가독성 점검'
  short_description: '맥락, 조건, JSX 흐름 기준 점검'
  default_prompt: '$frontend-readability-check로 현재 프론트엔드 diff의 가독성을 점검해줘.'
policy:
  allow_implicit_invocation: true
