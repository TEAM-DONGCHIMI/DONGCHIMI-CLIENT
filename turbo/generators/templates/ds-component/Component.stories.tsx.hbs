import { {{componentName}} } from './{{componentName}}';

const meta = {
  title: 'Design System/{{titleCase category}}/{{componentName}}',
  component: {{componentName}},
};

export default meta;

export const Default = {
  args: {
    children: '{{componentName}}',
  },
};
